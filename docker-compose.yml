version: '3.4'

services:
  php:
   build:
     context: .
     dockerfile: Dockerfile
   restart: always
   tty: true
   network_mode: 'bridge'
   volumes:
     - .:/var/www/html
   ports:
     - 8000:80